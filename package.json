{
  "name": "enzira-begena",
  "private": true,
  "workspaces": [
    "begena-app",
    "begena-backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=begena-app",
    "build": "npm run build --workspace=begena-app && rm -rf dist && cp -r begena-app/dist dist",
    "lint": "npm run lint --workspace=begena-app",
    "preview": "npm run preview --workspace=begena-app"
  }
}
